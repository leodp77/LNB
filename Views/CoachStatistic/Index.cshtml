@model IEnumerable<LNB.Models.CoachStatistic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Coach.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Game.GameId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TechnicalFouls)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Coach.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Game.GameId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TechnicalFouls)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoachStatisticId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoachStatisticId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoachStatisticId })
        </td>
    </tr>
}

</table>
