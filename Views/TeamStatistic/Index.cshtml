@model IEnumerable<LNB.Models.TeamStatistic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Game.GameId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Game.GameId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeamStatisticId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeamStatisticId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeamStatisticId })
        </td>
    </tr>
}

</table>
